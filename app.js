const WHATSAPP_BASE='https://wa.me/555432324898?text=';function openWhatsApp(message){window.open(WHATSAPP_BASE+encodeURIComponent(message),'_blank','noopener')}function productMessage(product){return product?`Olá, vim pelo site da Inovar Elevada. Gostaria de consultar: ${product}`:'Olá, vim pelo site da Inovar Elevada e gostaria de consultar um produto.'}const headerSearch=document.getElementById('headerSearch'),headerSearchInput=document.getElementById('headerSearchInput'),heroSearch=document.getElementById('heroSearch'),heroSearchInput=document.getElementById('heroSearchInput');headerSearch?.addEventListener('submit',e=>{e.preventDefault();openWhatsApp(productMessage(headerSearchInput.value.trim()))});heroSearch?.addEventListener('submit',e=>{e.preventDefault();openWhatsApp(productMessage(heroSearchInput.value.trim()))});document.querySelectorAll('[data-product]').forEach(el=>el.addEventListener('click',e=>{e.preventDefault();openWhatsApp(productMessage(el.dataset.product||''))}));const menuButton=document.getElementById('menuButton'),mainNav=document.getElementById('mainNav');function closeMenu(){mainNav?.classList.remove('open');document.body.classList.remove('menu-open');menuButton?.setAttribute('aria-expanded','false')}menuButton?.addEventListener('click',()=>{const open=!mainNav.classList.contains('open');mainNav.classList.toggle('open',open);document.body.classList.toggle('menu-open',open);menuButton.setAttribute('aria-expanded',String(open))});mainNav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));window.addEventListener('resize',()=>{if(innerWidth>720)closeMenu()});document.querySelectorAll('.faq-question').forEach(btn=>btn.addEventListener('click',()=>{const item=btn.closest('.faq-item'),wasOpen=item.classList.contains('open');document.querySelectorAll('.faq-item').forEach(i=>{i.classList.remove('open');i.querySelector('.faq-question')?.setAttribute('aria-expanded','false')});if(!wasOpen){item.classList.add('open');btn.setAttribute('aria-expanded','true')}}));const reveal=document.querySelectorAll('.reveal');if('IntersectionObserver'in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');io.unobserve(entry.target)}}),{threshold:.12});reveal.forEach(el=>io.observe(el))}else reveal.forEach(el=>el.classList.add('visible'));async function loadStories(){const area=document.getElementById('storyArea');if(!area)return;try{const r=await fetch(`./data/stories.json?ts=${Date.now()}`,{cache:'no-store'});if(!r.ok)return;const d=await r.json(),items=Array.isArray(d.items)?d.items:[];if(!items.length)return;const rail=document.createElement('div');rail.className='story-rail';items.forEach(item=>{const src=item.src||item.localUrl||item.url;if(!src)return;const card=document.createElement('div');card.className='story-card';const type=String(item.media_type||item.type||'').toUpperCase();if(type==='VIDEO'||/\.mp4(\?|$)/i.test(src)){const v=document.createElement('video');v.src=src;v.controls=true;v.playsInline=true;v.preload='metadata';card.appendChild(v)}else{const img=document.createElement('img');img.src=src;img.alt='Story da Inovar Elevada';img.loading='lazy';card.appendChild(img)}rail.appendChild(card)});if(rail.children.length){area.innerHTML='';area.appendChild(rail)}}catch(e){}}loadStories();setInterval(loadStories,300000);